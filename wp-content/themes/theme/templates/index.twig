{% extends 'base.twig' %}

{% block content %}
	<section class="pt-36 pb-12">
		<div class="container">
			<h1 class="text-3xl sm:text-4xl uppercase font-bold mb-8">
				{{ post.title }}
			</h1>
			<div class="prose prose-invert max-w-none">
				{{ post.content }}
			</div>
		</div>
	</section>
{% endblock %}
